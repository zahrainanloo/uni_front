import { AvailableCourses } from './available-courses.model';

describe('AvailableCourses', () => {
  it('should create an instance', () => {
    expect(new AvailableCourses()).toBeTruthy();
  });
});
